{
  "name": "chat-ncert-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --filter \"*\" dev",
    "build": "bun --filter \"*\" build",
    "lint": "bun --filter \"*\" lint",
    "test": "bun --filter \"*\" test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "1.8.3"
  }
}
